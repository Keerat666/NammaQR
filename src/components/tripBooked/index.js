import {View,Text,SafeAreaView,StyleSheet,TextInput, Button,Alert} from "react-native"

const TripBooked =(props, navigation)=>{

    return (<SafeAreaView>
        <View><Text>Trip booked!</Text></View></SafeAreaView>)
}

export default TripBooked